# CI verification

This branch exists to trigger the pull-request GitHub Actions workflow for SOL-CV32 v0.1.

The workflow must pass linting, unit tests, the official WhestBench estimator contract validator, a width-256 depth-32 smoke test, and submission archive packaging before this verification note is merged.
